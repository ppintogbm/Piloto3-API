apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculadora-api.fullname" . }}-test-connection"
  labels:
{{ include "calculadora-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "calculadora-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
